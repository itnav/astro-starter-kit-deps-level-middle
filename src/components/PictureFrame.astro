<div class="frame">
  <slot class="picture" />
</div>

<style scoped lang="scss">
  .frame {
    position: relative;
    display: inline-block;
    padding: 40px;
    overflow: hidden;
    background: #f5f5f5;
    background-image: linear-gradient(#e5e4df, #cdcdc6);
    border-color: #2f2d2d #434040 #4f4c4c #434040;
    border-style: solid;
    border-width: 20px;
    box-shadow:
      inset 0 2px 5px rgba(0, 0, 0, 0.6),
      0 5px 2px rgba(0, 0, 0, 0.1),
      0 10px 20px rgba(0, 0, 0, 0.8);

    & > ::global(img) {
      width: 240px;
      height: 160px;
      object-fit: cover;
      opacity: 0;
      @include transition(opacity, long4, ease-in-out, extra-long5);
    }

    & > ::global(img[src]) {
      opacity: 1;
    }
  }
</style>
