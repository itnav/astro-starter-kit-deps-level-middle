---
import PictureFrame from '@/components/PictureFrame.astro';

const imgUrl = await fetch('http://shibe.online/api/birds')
  .then((response) => response.json())
  .then((dataset) => dataset[0]);
---

<PictureFrame>
  <img src={imgUrl} alt="Random Bird Image" />
</PictureFrame>
